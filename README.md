# UsuariosApi

Projeto voltado para estudo de aplicação de Login de Usuários

Adicionando secrets na raiz do projeto com o comando:

dotnet user-secrets init

fazendo o set do secrets: 
dotnet user-screts set "SymmetricSecurityKey" "sua key"
